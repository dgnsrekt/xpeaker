{
  "name": "xpeaker",
  "version": "1.1.0",
  "description": "Read X/Twitter posts aloud in Chrome via chrome.tts + Supertonic voices, with on-device transformers.js AI.",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "wasm": "node copy-wasm.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.7.6"
  }
}
